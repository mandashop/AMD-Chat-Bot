# Checklist

- [x] RENDER 웹 서버가 정상적으로 실행되며 UptimeRobot 모니터링을 위한 `/ping` 엔드포인트가 동작하는가?
- [x] 그룹 채팅에서 사용자의 메시지가 정상적으로 카운트되고, `/rank` 조회 시 상위 10명이 정확히 노출되는가?
- [x] 그룹 채팅에서 출석체크 명령어(`ㅊㅊ`, `출첵`, `출석체크`)가 정상 인식되고 하루 1회만 처리되는가?
- [x] 환율 변환 메시지(예: `KRW40000>USDT`, `BTC0.1>KRW`) 입력 시 API를 통해 계산된 결과가 응답되는가?
- [x] 관리자가 봇이 포함된 그룹 채팅에서 `/admin` 명령어 입력 시 봇의 관리자 여부 및 유저의 정보 변경 권한을 확인한 후 관리자 메뉴가 나타나는가?
- [x] 동일 메시지 반복 방지 설정(시간 및 횟수), 금칙어 필터, 사용자명 변경 알림이 개별 그룹에 정상 적용되는가?
- [x] 예약 메시지 발송 기능이 그룹별로 독립적으로 지정한 시간에 반복 게시되는가?
- [x] 매일 자정 출석 초기화 및 매월 말일 그룹별 통계/출석 랭킹 공지가 자동으로 수행되도록 스케줄러가 등록되었는가?
- [x] 탈퇴 계정 추방 스케줄링이 각 그룹별로 관리자가 설정한 요일 및 시간에 동작하도록 구성되었는가?
- [x] `/backup` 또는 관리자 메뉴를 통해 해당 그룹의 통계 데이터를 `.TXT` 파일로 정상적으로 내보낼 수 있는가?
- [x] `config.py`가 `.env` 파일을 활용하여 토큰 및 API 키를 올바르게 로드하는가?
- [x] `.gitignore`, `README.md`, `.env` 템플릿 파일이 정상적으로 작성되어 Github 업로드 준비가 완료되었는가?